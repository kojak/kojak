#!/bin/bash
#
# Simple script to create a list of depedencies in GAV format

mvn -o dependency:list \
| grep ":.*:.*:.*" \
| cut -d] -f2- \
| sed 's/:[a-z]*$//g' \
| sed '/Finished/d'\
| sort -u > dep.list
